namespace Eventual.Domain
{
    /// <summary>
    /// Represents an Domain Event, something of import that has happened in
    /// the system.
    /// </summary>
    public class Event
    {
    }
}